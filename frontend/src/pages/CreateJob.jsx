import { useState } from 'react'
import api from '../api/axiosClient'
export default function CreateJob() { const [form,setForm]=useState({title:'',company_name:'',location:'',required_skills:'',required_experience:0,salary_range:'',description:'',status:'open'}); const [msg,setMsg]=useState(''); async function submit(e){e.preventDefault(); await api.post('/api/jobs',{...form, required_skills: form.required_skills.split(',').map(s=>s.trim()).filter(Boolean), required_experience:Number(form.required_experience)}); setMsg('Job created') } return <section><h1>Create Job</h1><form onSubmit={submit} className="gridform">{['title','company_name','location','required_skills','required_experience','salary_range'].map(k=><input key={k} placeholder={k} onChange={e=>setForm({...form,[k]:e.target.value})}/>)}<textarea placeholder="description" onChange={e=>setForm({...form,description:e.target.value})}/><button>Create</button></form><p>{msg}</p></section> }
